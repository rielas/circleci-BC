version: 2
jobs:
  build:
    docker:
      - image: circleci/node:12
    enviromment:
      SCAN_ID: test
    working_directory: ~/repo
    steps:
      - run: sudo npm install @neuralegion/nexploit-cli -g --unsafe-perm=true || true
      - run: echo $BASH_ENV 
      - run: test="test" 
      - run: echo $test
      - run: echo $SCAN_ID
      - run: printf "Scan was started with ID https://nexploit.app/scans/$SCAN_ID\n"
      - run: printf "Wait for issues ⏳\n"


